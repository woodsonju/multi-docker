version: "3"
services:
  client:
    image: woodson31/multi-client
    mem_limit: 128m
    hostname: client
  server:
    image: woodson31/multi-server
    mem_limit: 128m
    hostname: api
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
      - PGHOST=$PGHOST
      - PGUSER=$PGUSER
      - PGPASSWORD=$PGPASSWORD
      - PGDATABASE=$PGDATABASE
      - PGPORT=$PGPORT
  worker:
    image: woodson31/multi-worker
    mem_limit: 128m
    hostname: worker
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
  nginx:
    image: woodson31/multi-nginx
    mem_limit: 128m
    hostname: nginx
    ports:
      - "80:80"
